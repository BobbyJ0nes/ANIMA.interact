ANIMA.interact — the night
IU000325 Creative Making: Art & AI — submission bundle
Bilaal Auleear (bilaal.auleear@gmail.com)

WHAT THE PIECE IS
The author's 06.06.26 dream-writing, typeset as a walkable night in a
text-native software renderer (no meshes, no pixels — the third dimension
is an act of typesetting, re-performed every frame). An EEG recording of
the writing session itself sets the night's ambient weather, honesty-
labeled. Every displayed prose sentence is the author's journal,
verbatim, typos kept (the few utilitarian interface labels — the hint
line, the honesty stamps — are listed as such in the repository README).

HOW TO EXPERIENCE IT
1. HOSTED — nothing to install:
       https://bobbyj0nes.github.io/ANIMA.interact/
   The landing routes you into the night. Walk with wasd, look by
   dragging, space is the only button; the margin types the author's
   sentences as they become true — they are the instructions.
2. RUN IT YOURSELF — from the repository
   (https://github.com/BobbyJ0nes/ANIMA.interact), or identically from
   this bundle's code/ folder. Requirements: node 18+ and npm.
       cd code        (if using this bundle)
       npm install
       npm run dev        ->  http://localhost:5199
   The README's "run it" section (in code/ and the repository) maps
   every asset (the piece, the walker's guide, the lab view, the
   reference board, the studies).
3. Watch it: walk_v2.webm in this bundle — a captured 4.5-minute full
   walk, gate to fold.
4. Read it: GUIDE.pdf — part one walks the night stage by stage; part
   two documents the navigation premises, logic, and reasoning.

THIS BUNDLE
- [critical report PDF — the author's, added at upload]
- PROCESS.pdf — the iterative process record (timeline, method as
  constraint system, the AI/ML in this work, evidence index, AI-use
  statement)
- README_LOG.pdf — the living log: every study, verdict, and settled
  decision in chronological order
- GUIDE.pdf — the walker's guide + navigation design record
- walk_v2.webm — captured full-night walk (1280x720, WebM)
- code/ — the complete source of the piece (engine, scenes, data,
  pages, configs, docs). npm install; npm run dev inside it — identical
  to the repository.
- source/ — 06.06.26.txt (the spine entry, whole) and JOURNAL.md (the
  journal where it touches this night: censored verbatim excerpts,
  linked to their places in the piece)

REPOSITORY
Self-contained; the URL is given in the Moodle submission text field and
inside the report. Key files: README.md (the log), PROCESS.md, GUIDE.md,
src/term/ (engine + scenes + CONTRACT.md), source/06.06.26.txt (the
source writing, shipped as provenance), source/JOURNAL.md (the journal
where it touches this night — censored verbatim excerpts, linked to
their places in the piece), versions/the-night_v1/ (the frozen prior
version), shots/ (verification evidence).

AI-USE DECLARATION
All code was written by Claude (Fable 5) agent instances working under
the author's direction, constraint system, and in-session verification;
every source file carries a per-file declaration in its header. All
displayed prose is the author's own writing, verbatim (five lines from
journal entries beyond 06.06.26 are cited file:line in scene headers).
The critical report is written by the author alone. Full statement:
PROCESS.pdf, "ai-use statement".
