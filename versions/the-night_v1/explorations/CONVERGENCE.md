# convergence brief — after the seven-fork divergence · 19.08

> seven forks branched (water · sky · ground · beings · concepts ·
> submission · eeg-subconscious), all returned. twelve scenes now sweep
> clean (7 canon + 5 experiments in the `,`/`.` cycle). full docs sit
> beside this file; this is the synthesis. verdicts are bobby's.

## what came back, one line each

- **water** — five bays + the deep (`x_sea`): whisper/mirror/streams/
  wake/rain/tide; the boat sea's character is *refusal* (no wake, no
  glade — "There's never a splash")
- **sky** — six states (`x_sky`): fate's Pen constellation (one letter,
  never a word), letter-rain the ground remembers, the depletion arc
  ("they've all moved out" as structure), false dawn she follows
- **ground** — whisper moss built (`x_ground`): "leaning resolves" made
  mechanical; letter terraces as sediment; visible wind in tuft grass;
  capped worn trails
- **beings** — the doctrine (`x_beings`): **signs type, beings
  condense** — speech flies in from the sky and scatters on silence;
  gait/idle vocabulary; the mage gets nothing (refusal is his register)
- **concepts** — scenes-as-figures validated by his 31.07 hand; the
  18.05 rule sharpened (**actions locked** too); the pass rules are the
  dream's own design doc (the visitor paid with their tongue — mute);
  recommends **visitation-with-misrecognition**
- **submission** — brief found + quoted; form: hosted URL + walk video
  + mandatory repo; framing: representation-without-capture, agents-as-
  medium as method; per-code AI-use comments are *required and absent*
- **eeg** — 3 unnamed components = 96.8% variance; surrogate test:
  real 57.4% vs shuffled 18.6% (~3× coincidence — structure is
  genuine); `x_latent` built (the unshaped ground); rule proposed: no
  biosignal claim without its surrogate number

## adopt now (parent-owned, on bobby's word) — ranked

1. **signs type, beings condense** → port condensation to fate's
   dialogue + the middle otter (~40 scene-lines each, no engine change)
2. **whisper moss → gate** — the writing places it at the approach
3. **the wake** → gate/streetlamp/willow ring-seas; *never* boat
4. **engine: per-scene water hook** (~10 lines, gSample-pattern) —
   unlocks the whole water table + the shore condensation gradient
   (the thesis at every scene's edge)
5. **engine: depletion arc** — `dream.visited` drains the star
   fraction ~9%/scene toward the boat's 0.08
6. **engine: moon spec** ({phase, halo, waver} as data) + per-scene
   moon moods
7. second wave: engine `speak()` with sky provenance (~60 lines — the
   API shaped like a character-LLM token stream); `face()` fill hook
   (boat cliff as strata of old writing)
8. housekeeping wave: line-axis option + kill the legacy −1.2 offset
   (5 scene compensations adjusted with it); pass `cam` to `ground()`;
   CONTRACT additions (sky elevation band ≤0.30, `scenes/lib/`,
   engine-moon-uses-scene-ramp note)

## hold (bobby's call, not defaults)

- murmuration near-word ("gone", ~1.2s/40s) — english-rare budget
- the false dawn state — is dawn ever allowed in this dream?
- state-grammar weathers (eeg option b) — only if the piece claims
  subconscious representation at all
- whether `x_*` labs stay in the public `,`/`.` cycle or move behind a
  `?lab` flag

## the decision list — only bobby can answer

1. **whose "i"** — lock visitation-with-misrecognition? (concepts:
   yes; possession breaks at "Ismail's sister" and would claim his EEG
   as yours)
2. **the mute visitor** — tongue paid at the gate: mute everywhere?
   one exception at the mage's registration book?
3. **the two crossings** — display both witnesses (~28 poster vs
   ~50.5 pipeline) as two marks in the crossing scene, or re-derive
   until one confesses? (fable leans: display both — the disagreement
   *is* the honesty)
4. **the claim** — does the piece claim subconscious representation
   (then `x_latent` joins the circuit as the unshaped ground) or stay
   at weather?
5. **scope freeze** — submit as the constraint-system artwork it is,
   character-LLMs argued in the report as horizon? (submission fork:
   submittable without them)
6. **the EC date** — fetch it today; the two-scenario timeline in
   submission.md sequences everything off it

## parent actions queued on the word

git repo + hosting (~2h) · captured walk video (~3h) · per-code
AI-use comment pass (required by brief; scripted, cheap) · PROCESS.md
assembly (README + fork reports + shots → one document) · engine
waves 1→3 above

_provenance: fork reports + explorations/*.md; synthesis fable's;
nothing here is settled until bobby's verdicts land._
